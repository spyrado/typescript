// const message: string = 'Teste';
// message();
// message.toLocaleLowerCase();
/**
 This expression is not callable.
  Type 'String' has no call signatures.
 */
// Explicação:
// Ele está dizendo que a expressão message(); não é chamável, e de fato não é.
// Ele diz que o tipo string não tem nenhuma funcão vinculada a ele.
// Ou seja, uma propriedade do tipo string não consegue armazenar uma função, logo, se você tentar chamar message() vai dar erro.
// Porem Toda string tem uma serie de funções vinculadas a ela, como por exemplo message.toLowerCase();
